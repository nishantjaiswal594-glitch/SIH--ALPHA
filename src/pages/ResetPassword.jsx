import { useState } from 'react'; import { Link, useSearchParams } from 'react-router-dom'; import { api } from '../api/client'
export default function ResetPassword(){const [p]=useSearchParams();const token=p.get('token')||'';const [password,setPassword]=useState('');const [msg,setMsg]=useState('');const [error,setError]=useState('');const submit=async e=>{e.preventDefault();try{setMsg((await api.resetPassword({token,new_password:password})).message)}catch(e){setError(e.message)}};return <div className="auth-shell simple"><div className="auth-card"><h1>Choose a <span>new password.</span></h1><p>The reset token is read from the <code>?token=</code> URL parameter used by the backend.</p>{msg&&<div className="success-box">{msg}</div>}{error&&<div className="error-box">{error}</div>}<form onSubmit={submit}><label>New password<input type="password" minLength={6} value={password} onChange={e=>setPassword(e.target.value)} required /></label><button className="btn-primary full">Update password<span>→</span></button></form><div className="auth-foot"><Link to="/login">Return to sign in</Link></div></div></div>}

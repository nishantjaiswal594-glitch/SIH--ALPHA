import { useAuth } from '../context/AuthContext'; import { Link } from 'react-router-dom'; import { LogOut, ShieldCheck } from 'lucide-react'
export default function Settings(){const {user,logout}=useAuth();return <div><div className="page-kicker">SETTINGS</div><div className="title-row"><div><h1>Workspace <span>settings.</span></h1><p>Authentication and account controls for the current student session.</p></div></div><section className="panel settings-panel"><div className="setting-row"><div><strong>Signed in account</strong><span>{user?.email}</span></div><span className="setting-badge"><ShieldCheck size={14}/> {user?.role||'student'}</span></div><div className="setting-row"><div><strong>Email verification</strong><span>{user?.is_verified?'Verified':'Not verified'}</span></div><span className="setting-badge">{user?.is_verified?'✓':'!'}</span></div><div className="setting-row"><div><strong>Password</strong><span>Managed by backend auth endpoints.</span></div><Link to="/forgot-password" className="btn-secondary">Reset password</Link></div><div className="setting-row"><div><strong>Session</strong><span>Bearer token stored locally for API requests.</span></div><button className="btn-danger" onClick={logout}><LogOut size={14}/> Sign out</button></div></section></div>}

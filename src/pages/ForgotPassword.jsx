import { useState } from 'react'; import { Link } from 'react-router-dom'; import { api } from '../api/client'
export default function ForgotPassword(){const [email,setEmail]=useState('');const [msg,setMsg]=useState('');const [error,setError]=useState('');const [loading,setLoading]=useState(false);const submit=async e=>{e.preventDefault();setLoading(true);setError('');try{setMsg((await api.forgotPassword({email})).message)}catch(e){setError(e.message)}finally{setLoading(false)}};return <div className="auth-shell simple"><div className="auth-card"><h1>Reset your <span>password.</span></h1><p>Enter your email. The backend will send the reset link when the account exists.</p>{msg&&<div className="success-box">{msg}</div>}{error&&<div className="error-box">{error}</div>}<form onSubmit={submit}><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required /></label><button className="btn-primary full">{loading?'Sending…':'Send reset link'}<span>→</span></button></form><div className="auth-foot"><Link to="/login">Back to sign in</Link></div></div></div>}

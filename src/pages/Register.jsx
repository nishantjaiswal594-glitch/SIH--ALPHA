import { useState } from 'react'; import { Link, useNavigate } from 'react-router-dom'; import { useAuth } from '../context/AuthContext'; import { Sparkles } from 'lucide-react'
export default function Register(){ const {register}=useAuth(); const nav=useNavigate(); const [form,setForm]=useState({name:'',email:'',password:'',role:'student'}); const [msg,setMsg]=useState(''); const [error,setError]=useState(''); const [loading,setLoading]=useState(false); const change=e=>setForm({...form,[e.target.name]:e.target.value}); const submit=async e=>{e.preventDefault();setError('');setLoading(true);try{const r=await register(form);setMsg(r.message||'Registration successful. Check your email.')}catch(e){setError(e.message)}finally{setLoading(false)}}; return <div className="auth-shell simple"><div className="auth-card"><div className="auth-title"><Sparkles size={18}/><span>Create student account</span></div><h1>Start your <span>journey.</span></h1><p>Registration follows the backend contract and requires email verification before login.</p>{msg&&<div className="success-box">{msg}</div>}{error&&<div className="error-box">{error}</div>}<form onSubmit={submit}><label>Full name<input name="name" value={form.name} onChange={change} required /></label><label>Email<input name="email" type="email" value={form.email} onChange={change} required /></label><label>Password<input name="password" type="password" minLength={6} value={form.password} onChange={change} required /></label><input type="hidden" name="role" value="student"/><button className="btn-primary full" disabled={loading}>{loading?'Creating…':'Create account'}<span>→</span></button></form><div className="auth-foot">Already registered? <Link to="/login">Sign in</Link></div></div></div>}

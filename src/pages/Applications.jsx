import { useEffect, useState } from 'react'; import { Link } from 'react-router-dom'; import { api } from '../api/client'; import { FileWarning, Info, RefreshCcw } from 'lucide-react'
export default function Applications(){const [profile,setProfile]=useState(null);const [opp,setOpp]=useState(null);const [loading,setLoading]=useState(true);useEffect(()=>{Promise.allSettled([api.profile(),api.recommendedOpportunity()]).then(([p,o])=>{if(p.status==='fulfilled')setProfile(p.value);if(o.status==='fulfilled')setOpp(o.value)}).finally(()=>setLoading(false))},[]);if(loading)return <div className="loading-screen"><div className="spinner"/>Loading application data…</div>;return <div><div className="page-kicker">APPLICATION TRACKING</div><div className="title-row"><div><h1>Track your <span>applications.</span></h1><p>Connected to the real application action, with transparent API limitations.</p></div><div className="stat-pill">{profile?.statistics?.applications??0} recorded</div></div><div className="notice-card"><Info size={18}/><div><strong>The current backend does not expose an application-history endpoint.</strong><span>We will not fabricate a tracking table. The profile count is live, and new applications can be submitted from Opportunity Matching.</span></div></div><div className="grid-2"><section className="panel"><div className="panel-head"><h2>Live application summary</h2></div><div className="big-stat"><strong>{profile?.statistics?.applications??0}</strong><span>Total applications reported by <code>/users/me/profile</code></span></div><div className="status-row"><span>Application detail records</span><strong>Not exposed</strong></div><div className="status-row"><span>Current status history</span><strong>Not exposed</strong></div><div className="status-row"><span>Interview tracking</span><strong>Not exposed</strong></div></section><section className="panel"><div className="panel-head"><h2>Next supported action</h2></div>{opp?.recommendation?<div className="next-opportunity"><div><span>Top API-ranked opportunity</span><h3>{opp.recommendation.title}</h3><p>{opp.recommendation.company?.name} • {opp.recommendation.compatibility_score}% match</p></div><Link className="btn-primary" to={`/student/opportunities?opportunity=${opp.recommendation.opportunity_id}`}>Open <RefreshCcw size={14}/></Link></div>:<div className="empty">No recommendation available.</div>}</section></div><div className="api-note"><FileWarning size={14}/> To make the original tracking table fully live, the backend team would need a GET applications endpoint. This frontend intentionally reflects the current API contract.</div></div>}
